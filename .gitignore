# --- Python venvs ---
.venv/
.venv-tts/
__pycache__/
*.pyc

# --- Data & models (big / binary / regenerable — never commit) ---
# Exception: the production checkpoint (3 MB head + meta) is committed so CI can
# `modal deploy` — modal_app.py mounts models/sonave_xlsr_meet into the image.
data/
models/*
!models/sonave_xlsr_meet/
*.opus
*.wav
*.flac

# --- Generated results (keep the .md write-ups, drop heavy/regenerable stuff) ---
results/plots/
results/*.csv

# --- HF / torch caches if pointed local ---
.cache/
hf_cache/

# --- secrets (never commit) ---
.env

# --- per-deployment enrolled voiceprints ---
enrollments/

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
.ipynb_checkpoints/

# --- generated cards/reports (regenerable) ---
results/*.html

# --- local agent/session dirs ---
.claude/
scratch_*.json
