[pytest]
testpaths = tests
addopts = -q -m "not gpu"
markers =
    gpu: end-to-end test that loads the real model (slow; opt-in via `pytest -m gpu`)
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
