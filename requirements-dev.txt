# Dev/test tooling (on top of requirements.txt). Install into the detector env:
#   .venv\Scripts\python.exe -m pip install -r requirements-dev.txt
#
# Run the fast offline suite:   python -m pytest
# Include the model smoke test: python -m pytest -m gpu
pytest>=8.0
httpx>=0.27          # FastAPI TestClient transport
